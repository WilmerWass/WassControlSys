<UserControl x:Class="WassControlSys.Views.DiskHealthView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Salud de Discos" Style="{StaticResource HeaderTextStyle}" Margin="0,0,20,0"/>
            <Button Content="Refrescar" Command="{Binding RefreshDiskHealthCommand}" Style="{StaticResource SecondaryButtonStyle}"/>
        </StackPanel>
        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="20">
            <ListView ItemsSource="{Binding DiskHealth}" Background="Transparent" BorderThickness="0">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Dispositivo" Width="240" DisplayMemberBinding="{Binding DeviceId}"/>
                        <GridViewColumn Header="Modelo" Width="240" DisplayMemberBinding="{Binding Model}"/>
                        <GridViewColumn Header="Serial" Width="180" DisplayMemberBinding="{Binding Serial}"/>
                        <GridViewColumn Header="SMART" Width="120">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding SmartOk, Converter={StaticResource BooleanToStringConverter}}" Foreground="{Binding SmartOk, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=True}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>
    </Grid>
</UserControl>

