<UserControl x:Class="WassControlSys.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <TextBlock Text="{DynamicResource Settings_Title}" Style="{StaticResource HeaderTextStyle}" Margin="0,0,0,30"/>

        <!-- General Settings -->
        <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="25" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="{DynamicResource Settings_General}" FontSize="18" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,20"/>
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="{DynamicResource Settings_RunOnStartup}" Foreground="White" FontSize="15" Margin="0,0,0,5"/>
                        <TextBlock Text="{DynamicResource Settings_RunOnStartup_Desc}" Foreground="#888" FontSize="13"/>
                    </StackPanel>

                    <!-- Un CheckBox simple diseñado como interruptor podría ser complejo, usando un CheckBox estándar por ahora -->
                    <CheckBox Grid.Column="1" IsChecked="{Binding RunOnStartup}" VerticalAlignment="Center">
                        <CheckBox.LayoutTransform>
                            <ScaleTransform ScaleX="1.5" ScaleY="1.5"/>
                        </CheckBox.LayoutTransform>
                    </CheckBox>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Personalization -->
        <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="25">
            <StackPanel>
                <TextBlock Text="{DynamicResource Settings_Personalization}" FontSize="18" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,20"/>
                <TextBlock Text="{DynamicResource Settings_AccentColor}" Foreground="White" FontSize="15" Margin="0,0,0,15"/>
                
                <StackPanel Orientation="Horizontal">
                    <!-- Blue (Default) -->
                    <Button Command="{Binding ChangeAccentColorCommand}" CommandParameter="#3B82F6" Width="40" Height="40" Margin="0,0,15,0" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="#3B82F6" CornerRadius="20" BorderBrush="White" BorderThickness="0" ToolTip="Azul">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <!-- Green -->
                    <Button Command="{Binding ChangeAccentColorCommand}" CommandParameter="#10B981" Width="40" Height="40" Margin="0,0,15,0" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="#10B981" CornerRadius="20" BorderBrush="White" BorderThickness="0" ToolTip="Verde">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    
                    <!-- Red -->
                    <Button Command="{Binding ChangeAccentColorCommand}" CommandParameter="#EF4444" Width="40" Height="40" Margin="0,0,15,0" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="#EF4444" CornerRadius="20" BorderBrush="White" BorderThickness="0" ToolTip="Rojo">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    
                    <!-- Purple -->
                    <Button Command="{Binding ChangeAccentColorCommand}" CommandParameter="#8B5CF6" Width="40" Height="40" Margin="0,0,15,0" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="#8B5CF6" CornerRadius="20" BorderBrush="White" BorderThickness="0" ToolTip="Púrpura">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                     <!-- Orange -->
                    <Button Command="{Binding ChangeAccentColorCommand}" CommandParameter="#F97316" Width="40" Height="40" Margin="0,0,15,0" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="#F97316" CornerRadius="20" BorderBrush="White" BorderThickness="0" ToolTip="Naranja">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>

                <!-- Language -->
                <TextBlock Text="{DynamicResource Settings_Language}" Foreground="White" FontSize="15" Margin="0,20,0,10"/>
                <ComboBox SelectedValue="{Binding SelectedLanguage}" SelectedValuePath="Tag" Width="200">
                    <ComboBoxItem Content="{DynamicResource Language_Spanish}" Tag="es"/>
                    <ComboBoxItem Content="{DynamicResource Language_English}" Tag="en"/>
                    <ComboBox.ItemContainerStyle>
                        <Style TargetType="ComboBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        </Style>
                    </ComboBox.ItemContainerStyle>
                </ComboBox>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
