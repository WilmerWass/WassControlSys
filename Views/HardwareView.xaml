<UserControl x:Class="WassControlSys.Views.HardwareView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Hardware y Privacidad" Style="{StaticResource HeaderTextStyle}"/>

        <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0">
            <!-- Información General -->
            <TabItem Header="Información General">
                <Border Background="{DynamicResource SurfaceBrush}" CornerRadius="0,12,12,12" Padding="20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto" MinWidth="250"/>
                        </Grid.ColumnDefinitions>
                        
                        <ScrollViewer Grid.Column="0">
                            <StackPanel>
                                <TextBlock Text="Especificaciones Técnicas" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>
                                <ItemsControl ItemsSource="{Binding SystemInformation.GeneralItems}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,5">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="150"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="{Binding Key}" Foreground="{DynamicResource SecondaryTextBrush}"/>
                                                <TextBlock Text="{Binding Value}" Grid.Column="1" Foreground="{DynamicResource TextBrush}" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                
                                <Button Content="Exportar Backup de Drivers" Command="{Binding ExportDriversCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,20,0,0" HorizontalAlignment="Left"/>
                            </StackPanel>
                        </ScrollViewer>

                        <!-- Panel Lateral de Seguridad -->
                        <Border Grid.Column="1" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource SecondaryTextBrush}" BorderThickness="1" CornerRadius="12" Padding="15" Margin="15,0,0,0">
                            <StackPanel>
                                <TextBlock Text="Seguridad" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                                <TextBlock Text="Antivirus:" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,5,0,0"/>
                                <TextBlock Text="{Binding SecurityStatus.Antivirus}" Foreground="{DynamicResource TextBrush}" FontWeight="Bold"/>
                                <Separator Background="{DynamicResource SecondaryTextBrush}" Opacity="0.2" Margin="0,10"/>
                                <TextBlock Text="Firewall:" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,5,0,0"/>
                                <TextBlock Text="{Binding SecurityStatus.Firewall}" Foreground="{DynamicResource TextBrush}" FontWeight="Bold"/>
                                <Separator Background="{DynamicResource SecondaryTextBrush}" Opacity="0.2" Margin="0,10"/>
                                <TextBlock Text="Windows Update:" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,5,0,0"/>
                                <TextBlock Text="{Binding SecurityStatus.WindowsUpdate}" Foreground="{DynamicResource TextBrush}" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Border>
            </TabItem>

            <!-- Discos -->
            <TabItem Header="Discos">
                <Border Background="{DynamicResource SurfaceBrush}" CornerRadius="0,12,12,12" Padding="20">
                    <ScrollViewer>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Sección Superior: Lista de Discos -->
                            <StackPanel Grid.Row="0">
                                <Grid Margin="0,0,0,15">
                                    <TextBlock Text="Información de Discos" FontSize="16" FontWeight="Bold" HorizontalAlignment="Left"/>
                                    <Button Content="Actualizar" 
                                            Command="{Binding RefreshDiskHealthCommand}" 
                                            Style="{StaticResource SecondaryButtonStyle}" 
                                            HorizontalAlignment="Right"
                                            Padding="15,5"/>
                                </Grid>
                                
                                <DataGrid ItemsSource="{Binding DiskHealth}" 
                                          AutoGenerateColumns="False" 
                                          Background="Transparent" 
                                          Foreground="{DynamicResource TextBrush}" 
                                          BorderThickness="0" 
                                          CanUserAddRows="False"
                                          HeadersVisibility="Column"
                                          GridLinesVisibility="Horizontal"
                                          RowBackground="{DynamicResource SurfaceBrush}"
                                          AlternatingRowBackground="{DynamicResource WindowBackgroundBrush}"
                                          Margin="0,0,0,20">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Disco" Binding="{Binding DeviceId}" Width="150"/>
                                        <DataGridTextColumn Header="Modelo" Binding="{Binding Model}" Width="2*"/>
                                        <DataGridTextColumn Header="Capacidad" Binding="{Binding Capacity}" Width="150"/>
                                        <DataGridTemplateColumn Header="SMART" Width="100">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBlock FontWeight="Bold" HorizontalAlignment="Center">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                                                                <Setter Property="Text" Value="{Binding SmartStatus}"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding SmartOk}" Value="True">
                                                                        <Setter Property="Foreground" Value="#10B981"/>
                                                                        <Setter Property="Text" Value="✓ OK"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding SmartOk}" Value="False">
                                                                        <Setter Property="Foreground" Value="#EF4444"/>
                                                                        <Setter Property="Text" Value="✗ Error"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding SmartStatusKnown}" Value="False">
                                                                        <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}"/>
                                                                        <Setter Property="Text" Value="{Binding SmartStatus}"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </StackPanel>

                            <!-- Sección Inferior: Analizadores de Espacio -->
                            <Grid Grid.Row="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Analizador de Espacio C:\ -->
                                <Border Grid.Column="0" Background="{DynamicResource WindowBackgroundBrush}" CornerRadius="8" Padding="15">
                                    <StackPanel>
                                        <TextBlock Text="Analizador de Espacio" FontSize="14" FontWeight="Bold" Margin="0,0,0,10"/>
                                        <Button Content="Analizar C:\" 
                                                Command="{Binding AnalyzeDiskSpaceCommand}" 
                                                CommandParameter="C:\" 
                                                Style="{StaticResource SecondaryButtonStyle}" 
                                                HorizontalAlignment="Left" 
                                                Margin="0,0,0,10"/>
                                        <ScrollViewer MaxHeight="300" VerticalScrollBarVisibility="Auto">
                                            <ItemsControl ItemsSource="{Binding DiskAnalysisResult}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Margin="0,5">
                                                            <Grid>
                                                                <TextBlock Text="{Binding Path}" Foreground="{DynamicResource TextBrush}" FontSize="12"/>
                                                                <TextBlock Text="{Binding FormattedSize}" Foreground="{DynamicResource SecondaryTextBrush}" HorizontalAlignment="Right" FontSize="12"/>
                                                            </Grid>
                                                            <ProgressBar Value="{Binding Percentage}" Maximum="100" Height="6" Margin="0,4,0,0" Background="{DynamicResource WindowBackgroundBrush}" Foreground="{DynamicResource AccentBrush}" BorderThickness="0"/>
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </ScrollViewer>
                                    </StackPanel>
                                </Border>

                                <!-- Analizador de Espacio D:\ -->
                                <Border Grid.Column="2" Background="{DynamicResource WindowBackgroundBrush}" CornerRadius="8" Padding="15">
                                    <StackPanel>
                                        <TextBlock Text="Analizador de Espacio" FontSize="14" FontWeight="Bold" Margin="0,0,0,10"/>
                                        <Button Content="Analizar D:\" 
                                                Command="{Binding AnalyzeDiskSpaceCommand}" 
                                                CommandParameter="D:\" 
                                                Style="{StaticResource SecondaryButtonStyle}" 
                                                HorizontalAlignment="Left" 
                                                Margin="0,0,0,10"/>
                                        <ScrollViewer MaxHeight="300" VerticalScrollBarVisibility="Auto">
                                            <ItemsControl ItemsSource="{Binding DiskAnalysisResultD}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Margin="0,5">
                                                            <Grid>
                                                                <TextBlock Text="{Binding Path}" Foreground="{DynamicResource TextBrush}" FontSize="12"/>
                                                                <TextBlock Text="{Binding FormattedSize}" Foreground="{DynamicResource SecondaryTextBrush}" HorizontalAlignment="Right" FontSize="12"/>
                                                            </Grid>
                                                            <ProgressBar Value="{Binding Percentage}" Maximum="100" Height="6" Margin="0,4,0,0" Background="{DynamicResource WindowBackgroundBrush}" Foreground="{DynamicResource AccentBrush}" BorderThickness="0"/>
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </ScrollViewer>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Grid>
                    </ScrollViewer>
                </Border>
            </TabItem>

            <!-- Privacidad -->
            <TabItem Header="Privacidad">
                <Border Background="{DynamicResource SurfaceBrush}" CornerRadius="0,12,12,12" Padding="20">
                    <ScrollViewer>
                        <ItemsControl ItemsSource="{Binding PrivacySettings}">
                            <ItemsControl.ItemTemplate>
                                 <DataTemplate>
                                    <Border BorderBrush="{DynamicResource SecondaryTextBrush}" Opacity="0.2" BorderThickness="0,0,0,1" Padding="10">
                                        <Grid>
                                            <StackPanel Margin="0,0,100,0">
                                                <TextBlock Text="{Binding Name}" Foreground="{DynamicResource TextBrush}" FontWeight="Bold"/>
                                                <TextBlock Text="{Binding Description}" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" TextWrapping="Wrap"/>
                                            </StackPanel>
                                            <ToggleButton HorizontalAlignment="Right" VerticalAlignment="Center" 
                                                          IsChecked="{Binding IsEnabled}"
                                                          Command="{Binding DataContext.UpdatePrivacySettingCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                          CommandParameter="{Binding}"
                                                          Style="{StaticResource ToggleSwitchStyle}"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
